// 性能策略固定值：不暴露给前端设置，避免用户误改导致连接与渲染行为不一致。
export const TERMINAL_SCROLLBACK = 5000;
export const TERMINAL_WEBGL_ENABLED = true;
export const LOW_RESOURCE_MODE_ENABLED = true;
export const INACTIVE_TELEMETRY_INTERVAL_MS = 30_000;
export const AUTO_RECONNECT_ENABLED = true;
export const AUTO_RECONNECT_MAX_ATTEMPTS = 5;
